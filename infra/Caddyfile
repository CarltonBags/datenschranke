# Prod reverse proxy with automatic TLS. `deploy.sh prod` sets PUBLIC_DOMAIN.
{$PUBLIC_DOMAIN:localhost} {
	handle /v1/* {
		reverse_proxy gateway:8080
	}
	handle /api/* {
		reverse_proxy gateway:8080
	}
	handle {
		reverse_proxy web:3000
	}
}
